---
modules: [GEOAPI, GEOWEB, REURBCAD, GEOGIS, WEBDOCS]
epic: "category"
priority: "critical|high|medium|low"
story_points: 0
---

# RF-NNN-nome-funcionalidade

Descrição completa funcionalidade detalhando ator executando operação objetivo alcançado validações aplicadas regras negócio verificadas exceções tratadas integrações componentes externos invocados fluxo dados entrada processamento saída formato esperado HTTP status codes responses payloads JSON schemas validações campo obrigatório opcional tipo string number boolean formato email CPF CNPJ date datetime UUID constraints mínimo máximo length pattern regex autenticação requerida Authorization header Bearer token JWT claims roles permissions RBAC validando usuário autorizado ação recurso tenant isolation RLS Row Level Security PostgreSQL filtrando automaticamente tenant_id evitando vazamento dados cross-tenant security breach multi-tenancy garantido arquitetura camadas Clean Architecture domain entities aggregates value objects application use cases CQRS commands queries handlers validators infrastructure repositories database Entity Framework Core migrations schema atualizado deployment automático CI/CD pipeline GitHub Actions build test deploy staging smoke tests promoting production blue-green deployment zero downtime canary releases gradual rollout monitoring error rates latency health checks readiness liveness probes Kubernetes pods replicas scaling horizontal vertical HPA metrics-based autoscaling CPU memory custom metrics request rate queue length.

## Critérios de Aceitação

- Given [contexto pré-condição estado inicial] When [ação usuário trigger evento] Then [resultado esperado validação observable]
- Campo validado formato aceito response HTTP code correto
- Erro retornado mensagem clara descritiva acionável usuário corrigir input
- Audit log registrado operação timestamp user_id tenant_id entity_id action details compliance LGPD rastreabilidade

## Rastreabilidade

- **Use Cases**: UC-NNN-relacionado
- **User Stories**: US-NNN-derivada
- **Business Rules**: RN-NNN-aplicada
- **Technical Specs**: [link documentação técnica implementação]

## Notas de Implementação

Detalhes técnicos bibliotecas sugeridas NetTopologySuite spatial geometries FluentValidation rules MediatR domain events Hangfire background jobs Serilog structured logging Npgsql PostgreSQL provider endpoints API controllers /api/resource POST PUT PATCH DELETE tabelas database afetadas units holders communities legitimation_requests colunas indexes constraints foreign keys unique spatial GiST indexes performance otimização queries execution plans EXPLAIN ANALYZE identificando bottlenecks.

---

**Última atualização:** YYYY-MM-DD
