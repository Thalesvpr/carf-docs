# PROVISIONING

Grafana provisioning automático. datasources.yml (Prometheus, Loki, PostgreSQL). dashboards.yml (path /etc/grafana/dashboards/*.json auto-reload). notifiers.yml (Slack webhook, email SMTP, PagerDuty). Deployed via ConfigMaps Kubernetes ou volumes Docker.

---

**Última atualização:** 2025-12-29
